# ── LLM Provider ──────────────────────────
# SYPHER_LLM_PAID_TIER=true
#   Set to true/1/yes when using a paid API plan (Gemini, OpenAI, etc.).
#   Disables LLM rate limits (default 2 per 15 sec). Free tier: leave unset or false.
# Set the API key for your provider (at least one required)
# CEREBRAS_API_KEY=xxx
# OPENAI_API_KEY=sk-xxx
# ANTHROPIC_API_KEY=sk-ant-xxx
# GEMINI_API_KEY=xxx
#   Used by: Gemini API provider (providers.gemini) and Gemini CLI (gemini-cli agent)
#   Default model: gemini-2.5-flash-lite (unless changed via GEMINI_MODEL or config)
#   Get key: https://aistudio.google.com/
# GEMINI_MODEL=gemini-2.5-flash-lite
#   Overrides agents.defaults.model when GEMINI_API_KEY is set

# ── WhatsApp ──────────────────────────────
# For bridge: connect to ws://localhost:3001
# For Baileys: extension runs on port 3002, set use_baileys in config

# ── Gateway ───────────────────────────────
# SYPHER_GATEWAY_URL=http://localhost:18790

# ── Timezone ──────────────────────────────
# TZ=UTC
